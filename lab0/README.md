# IPC144 Code Answer
This directory contains the coded solution to the lab. **Lab0** will have a single `.c` source code file (**`lab0.c`**).

If you are using Visual Studio, there will be additional files of type:
* `*.vcxproj`
* `*.sln`

> [!NOTE]
>
> Regardless of the IDE you choose to use or the extra files that environment creates, the files that matter when you submit your work will be those with the `*.c`, `*.h` files extensions. Later in the course data files could also be required as per the specific lab instructions.